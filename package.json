{
  "name": "rec-league-app",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd rec-league-client && npm install && npm build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm build && npm deploy && echo 'Client built!'"
  }
}
